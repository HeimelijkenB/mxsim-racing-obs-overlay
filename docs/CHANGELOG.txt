MxSim Racing OBS Overlay v2.0.12
- Fixed league row parsing when the site omits spaces (e.g. tier + race count + "races" + ELO glued together), which caused false "player not found" results.
- Broader "load more" button labels (FR/DE/NL) for expanding long standings.

MxSim Racing OBS Overlay v2.0.11
- Fixed rider parsing regression where a matched name could be found without full rank/rating/class/races.
- Improved DOM row selection: app now prefers the full ranking card/row instead of the shortest name-only element.
- Added robust partial-name matching across row extraction and text parsing.
- Browser scrape now continues loading more until complete ranking data is found, not only until a name appears.
- Avatar extraction remains optional and cannot break ranking detection.
- Kept v2.0.9 cleanup behavior and file-sync overlay update.
